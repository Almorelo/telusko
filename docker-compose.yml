version: '3'

services:
  web:
    build: .  # Buildujeme current folder
    command: python manage.py runserver 0.0.0.0:8066    # Je jedno jaký port
    volumes:
      - .:/app
    ports:
      - "8066:8066"


